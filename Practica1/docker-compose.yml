services:
  vite-app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules_data:/app/node_modules
    stdin_open: true
    tty: true

volumes:
  node_modules_data:
